//
//  HighscoreController.swift
//  Rolleo Mapeo
//
//  Created by Mathias I Nielsen on 25/10/2018.
//  Copyright © 2018 Mathias I Nielsen. All rights reserved.
//

import UIKit

class HighscoreController: UIViewController {

    //@IBOutlet weak var highscoreTable: UITableView!
    //var highScoreArray = UserDefaults.stringArray(Userdefault) as [String]!
    
    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }
    /*
     func numberOfSections(in highscoreTable: UITableView) -> Int {
        return 1
    }
    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
        return highScoreArray.count
    }
    
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        let cell = tableView.dequeueReusableCell(withIdentifier: "TableViewCell")!
        cell.textLabel?.text = self.CategoryArray[indexPath.row]
        return cell
    }
     */
    
    /*@IBOutlet weak var tableView: UITableView!
    
    func highScoreFiller(highscores: [Double]) -> Int {
        return highscores.count
    }
    
    func setupTableView() -> UITableViewCell{
        let cell = tableView.cellForRow(at: <#T##IndexPath#>)
        return
    }
    */
    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
